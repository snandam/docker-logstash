###### To create a docker image for a particular version to run rspec tests

```sh
docker build  -t="gunmetalz/logstash:2.1" .
docker push
```

###### Docker hub
https://hub.docker.com/r/gunmetalz/logstash/
